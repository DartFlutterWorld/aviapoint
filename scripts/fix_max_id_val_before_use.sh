#!/bin/bash

# Скрипт для перемещения вычисления max_id_val ДО использования
# Использование: выполните на сервере

echo "🔧 Перемещение вычисления max_id_val ДО использования..."
echo ""
echo "Выполните на сервере:"
echo ""
echo "cd /home/aviapoint_server"
echo ""
echo "# Проблема: max_id_val используется на строке 996, но вычисляется на 1009"
echo "# Решение: переместить вычисление ДО UPDATE"
echo ""
echo "# 1. Посмотреть полный контекст блока IF duplicate_count > 0"
echo "sed -n '985,1015p' migrations/071_add_content_and_images_to_news.sql"
echo ""
echo "# 2. Нужно добавить вычисление max_id_val ПЕРЕД UPDATE (перед строкой 992)"
echo "# Вставить после строки 989 (после 'IF duplicate_count > 0 THEN'):"
echo "#     SELECT COALESCE(MAX(id), 0) INTO max_id_val FROM news;"
echo ""
echo "# 3. Удалить дублирующее вычисление на строке 1009 (оно уже не нужно)"
echo ""
