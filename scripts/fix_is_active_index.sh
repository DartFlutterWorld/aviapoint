#!/bin/bash

# Скрипт для исправления проблемы с индексом is_active
# Использование: выполните на сервере

echo "🔧 Исправление проблемы с индексом is_active..."
echo ""
echo "Выполните на сервере:"
echo ""
echo "cd /home/aviapoint_server"
echo ""
echo "# 1. Найти миграцию, которая создаёт индекс на is_active"
echo "grep -rn 'idx_aircraft_manufacturers_active' migrations/*.sql"
echo ""
echo "# 2. Проверить, есть ли миграция, которая создаёт колонку is_active"
echo "grep -rn 'ADD COLUMN.*is_active\\|is_active.*ADD\\|is_active.*BOOLEAN' migrations/*.sql | grep aircraft_manufacturers"
echo ""
echo "# 3. Решение: обернуть создание индекса в проверку существования колонки"
echo "# Или добавить создание колонки перед созданием индекса"
echo ""
